server:
  port: 80
spring:
  datasource:
    password: root123
    username: postgres
    url: jdbc:postgresql://localhost:5432/rest_api
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
